# Neovim 기본 사용 단축키

> Neovim의 내장 명령어 치트시트입니다. 이 파일은 플러그인이나 커스텀 매핑과 무관하게 Vim/Neovim 자체가 제공하는 기본 기능을 정리합니다.

---

## 목차
1. [모드 전환](#1-모드-전환)
2. [커서 이동](#2-커서-이동)
3. [화면 스크롤](#3-화면-스크롤)
4. [편집 명령](#4-편집-명령)
5. [삭제 / 복사 / 붙여넣기](#5-삭제--복사--붙여넣기)
6. [되돌리기 / 다시 실행](#6-되돌리기--다시-실행)
7. [검색과 치환](#7-검색과-치환)
8. [텍스트 오브젝트](#8-텍스트-오브젝트)
9. [비주얼 모드](#9-비주얼-모드)
10. [마크(Mark)](#10-마크mark)
11. [레지스터(Register)](#11-레지스터register)
12. [매크로(Macro)](#12-매크로macro)
13. [창(Window) 관리](#13-창window-관리)
14. [탭(Tab) 관리](#14-탭tab-관리)
15. [버퍼(Buffer) 관리](#15-버퍼buffer-관리)
16. [폴딩(Folding)](#16-폴딩folding)
17. [명령 모드(Command-line)](#17-명령-모드command-line)
18. [파일 관련](#18-파일-관련)
19. [도움말](#19-도움말)

---

## 1. 모드 전환

| 단축키 | 설명 |
|--------|------|
| `i` | 커서 앞에서 입력 모드 전환 |
| `I` | 커서를 줄의 첫 글자 앞으로 이동, 입력 모드 전환 |
| `a` | 커서 뒤에서 입력 모드 전환 (append) |
| `A` | 커서를 줄 끝으로 이동, 입력 모드 전환 |
| `o` | 아래에 새 줄 삽입 후 입력 모드 전환 |
| `O` | 위에 새 줄 삽입 후 입력 모드 전환 |
| `s` | 커서 위치 글자 삭제 후 입력 모드 전환 |
| `S` | 현재 줄 전체 삭제 후 입력 모드 전환 |
| `v` | 비주얼 모드 전환 (글자 단위 선택) |
| `V` | 비주얼 라인 모드 전환 (줄 단위 선택) |
| `<C-v>` | 비주얼 블록 모드 전환 (사각형 영역 선택) |
| `R` | 교체(Replace) 모드 전환, 텍스트 덮어쓰기 |
| `:` | 명령 모드(Command-line) 전환 |
| `<Esc>` 또는 `<C-[>` | 노멀 모드로 복귀, 모드 전환 취소 |

---

## 2. 커서 이동

### 기본 이동
| 단축키 | 설명 |
|--------|------|
| `h` / `j` / `k` / `l` | 커서 왼쪽 / 아래 / 위 / 오른쪽 이동 |
| `0` | 커서를 줄의 맨 앞으로 이동 (들여쓰기 포함) |
| `^` | 커서를 줄의 첫 글자로 이동 (들여쓰기 제외) |
| `$` | 커서를 줄의 맨 끝으로 이동 |
| `g_` | 커서를 줄의 마지막 공백이 아닌 글자로 이동 |

### 단어 단위 이동
| 단축키 | 설명 |
|--------|------|
| `w` | 커서를 다음 단어의 시작으로 이동 |
| `W` | 커서를 다음 단어(공백 기준)의 시작으로 이동 |
| `e` | 커서를 현재/다음 단어의 끝으로 이동 |
| `E` | 커서를 현재/다음 단어(공백 기준)의 끝으로 이동 |
| `b` | 커서를 이전 단어의 시작으로 이동 |
| `B` | 커서를 이전 단어(공백 기준)의 시작으로 이동 |
| `ge` | 커서를 이전 단어의 끝으로 이동 |

### 문서 단위 이동
| 단축키 | 설명 |
|--------|------|
| `gg` | 커서를 파일 맨 위로 이동 |
| `G` | 커서를 파일 맨 아래로 이동 |
| `{숫자}G` | 커서를 지정한 줄 번호로 이동 (예: `42G`) |
| `:{숫자}` | 커서를 지정한 줄 번호로 이동 (예: `:42`) |
| `%` | 커서를 짝이 되는 괄호로 이동 `()` `[]` `{}` |
| `(` / `)` | 커서를 이전 / 다음 문장 시작으로 이동 |
| `{` / `}` | 커서를 이전 / 다음 문단 시작으로 이동 |

### 화면 내 이동
| 단축키 | 설명 |
|--------|------|
| `H` | 커서를 화면 맨 위로 이동 (High) |
| `M` | 커서를 화면 중간으로 이동 (Middle) |
| `L` | 커서를 화면 맨 아래로 이동 (Low) |

### 글자 검색 이동
| 단축키 | 설명 |
|--------|------|
| `f{글자}` | 커서를 같은 줄 앞쪽의 해당 글자로 이동 (찾기) |
| `F{글자}` | 커서를 같은 줄 뒤쪽의 해당 글자로 이동 (역방향 찾기) |
| `t{글자}` | 커서를 앞쪽 해당 글자 직전으로 이동 (till) |
| `T{글자}` | 커서를 뒤쪽 해당 글자 직후로 이동 (역방향 till) |
| `;` | 직전 `f`/`t` 글자 검색 반복 (같은 방향) |
| `,` | 직전 `f`/`t` 글자 검색 반복 (반대 방향) |

### 점프 이력
| 단축키 | 설명 |
|--------|------|
| `<C-o>` | 커서를 이전 점프 위치로 이동 (뒤로) |
| `<C-i>` | 커서를 다음 점프 위치로 이동 (앞으로) |
| `''` | 커서를 직전 점프 위치로 이동 |
| `` `` `` | 커서를 직전 편집 위치로 이동 |

### 입력 모드(Insert Mode) 커서 이동
| 단축키 | 설명 |
|--------|------|
| `<S-Right>` | 커서를 다음 단어로 이동 (Shift + →) |
| `<S-Left>` | 커서를 이전 단어로 이동 (Shift + ←) |
| `<C-Right>` | 커서를 다음 WORD로 이동 (Ctrl + →, 공백 기준) |
| `<C-Left>` | 커서를 이전 WORD로 이동 (Ctrl + ←, 공백 기준) |
| `<Home>` | 커서를 줄의 맨 앞으로 이동 |
| `<End>` | 커서를 줄의 맨 끝으로 이동 |
| `<C-o>{모션}` | 노멀 모드 명령 1회 실행 후 입력 모드 복귀 (커서 이동 등) |

> **팁**: `<C-o>`를 활용하면 노멀 모드의 모든 이동 명령을 입력 모드에서 사용할 수 있습니다.
> 예: `<C-o>w` (다음 단어), `<C-o>b` (이전 단어), `<C-o>$` (줄 끝), `<C-o>gg` (파일 맨 위)

---

## 3. 화면 스크롤

| 단축키 | 설명 |
|--------|------|
| `<C-d>` | 화면 반 페이지 아래로 스크롤, 커서 이동 |
| `<C-u>` | 화면 반 페이지 위로 스크롤, 커서 이동 |
| `<C-f>` | 화면 한 페이지 아래로 스크롤 (Forward) |
| `<C-b>` | 화면 한 페이지 위로 스크롤 (Backward) |
| `<C-e>` | 화면 한 줄 아래로 스크롤 (커서 위치 유지) |
| `<C-y>` | 화면 한 줄 위로 스크롤 (커서 위치 유지) |
| `zz` | 커서가 있는 현재 줄을 화면 중앙에 배치 |
| `zt` | 커서가 있는 현재 줄을 화면 상단에 배치 |
| `zb` | 커서가 있는 현재 줄을 화면 하단에 배치 |

---

## 4. 편집 명령

| 단축키 | 설명 |
|--------|------|
| `r{글자}` | 커서 위치 글자를 해당 글자로 교체 (편집) |
| `R` | 교체 모드 전환, `<Esc>`까지 텍스트 덮어쓰기 (편집) |
| `~` | 커서 위치 글자의 대소문자 전환 (편집) |
| `gU{모션}` | 텍스트 영역을 대문자로 변환 (예: `gUiw`) |
| `gu{모션}` | 텍스트 영역을 소문자로 변환 (예: `guiw`) |
| `g~{모션}` | 텍스트 영역 대소문자 토글 |
| `>>` | 현재 줄 들여쓰기 증가 (인덴트, 편집) |
| `<<` | 현재 줄 들여쓰기 감소 (아웃덴트, 편집) |
| `==` | 현재 줄 자동 들여쓰기 (포맷) |
| `gg=G` | 파일 전체 자동 들여쓰기 (포맷) |
| `J` | 다음 줄을 현재 줄에 이어 붙임 (줄 합치기) |
| `gJ` | 공백 없이 다음 줄 이어 붙임 (줄 합치기) |
| `<C-a>` | 커서 위치의 숫자 증가 (편집) |
| `<C-x>` | 커서 위치의 숫자 감소 (편집) |
| `.` | 직전 편집 동작 반복 (리피트) |

---

## 5. 삭제 / 복사 / 붙여넣기

> **핵심 패턴**: `{오퍼레이터}{모션}` 조합입니다. `d`(삭제), `y`(복사), `c`(변경) 뒤에 어떤 모션이 오느냐에 따라 동작 범위가 결정됩니다. 예를 들어 `d$`는 "커서부터 줄 끝까지 삭제", `y0`는 "커서부터 줄 시작까지 복사"가 됩니다.

### 삭제 (Delete)
| 단축키 | 설명 |
|--------|------|
| `x` | 커서 위치 한 글자 삭제 (텍스트 지우기) |
| `X` | 커서 앞 한 글자 삭제 (백스페이스) |
| `dd` | 현재 줄 전체 삭제 (텍스트 지우기) |
| `D` 또는 `d$` | 커서부터 줄 끝까지 텍스트 삭제 |
| `d0` | 커서부터 줄 시작(들여쓰기 포함)까지 텍스트 삭제 |
| `d^` | 커서부터 줄 첫 글자(들여쓰기 제외)까지 텍스트 삭제 |
| `dw` | 커서부터 다음 단어 시작까지 텍스트 삭제 |
| `dW` | 커서부터 다음 단어(공백 기준) 시작까지 텍스트 삭제 |
| `de` | 커서부터 현재 단어 끝까지 텍스트 삭제 |
| `db` | 커서부터 이전 단어 시작까지 텍스트 삭제 |
| `dj` | 현재 줄과 아래 줄 삭제 |
| `dk` | 현재 줄과 위 줄 삭제 |
| `dgg` | 커서부터 파일 처음까지 텍스트 삭제 |
| `dG` | 커서부터 파일 끝까지 텍스트 삭제 |
| `d%` | 커서부터 짝이 되는 괄호까지 텍스트 삭제 |
| `df{글자}` | 커서부터 해당 글자까지 텍스트 삭제(포함) |
| `dt{글자}` | 커서부터 해당 글자 직전까지 텍스트 삭제 |
| `d/{패턴}` | 커서부터 검색 패턴 직전까지 텍스트 삭제 |
| `diw` / `daw` | 현재 단어 텍스트 삭제 (안쪽 / 공백 포함) |
| `di"` / `da"` | 따옴표 안쪽 / 바깥쪽 텍스트 삭제 |
| `di(` / `da(` | 괄호 안쪽 / 바깥쪽 텍스트 삭제 |
| `di{` / `da{` | 중괄호 안쪽 / 바깥쪽 텍스트 삭제 |
| `dip` / `dap` | 문단 안쪽 / 바깥쪽 텍스트 삭제 |
| `dit` / `dat` | HTML 태그 안쪽 / 바깥쪽 텍스트 삭제 |
| `{숫자}dd` | N줄 텍스트 삭제 (예: `3dd`는 3줄 삭제) |

### 복사 (Yank)
| 단축키 | 설명 |
|--------|------|
| `yy` | 현재 줄 전체 텍스트 복사 (yank) |
| `Y` | 커서부터 줄 끝까지 텍스트 복사 (Neovim 기본, `y$`와 동일) |
| `y$` | 커서부터 줄 끝까지 텍스트 복사 |
| `y0` | 커서부터 줄 시작까지 텍스트 복사 |
| `y^` | 커서부터 줄 첫 글자까지 텍스트 복사 |
| `yw` / `yW` | 커서부터 다음 단어 시작까지 텍스트 복사 |
| `ye` | 커서부터 현재 단어 끝까지 텍스트 복사 |
| `yb` | 커서부터 이전 단어 시작까지 텍스트 복사 |
| `yj` / `yk` | 현재 줄과 아래 / 위 줄 텍스트 복사 |
| `ygg` | 커서부터 파일 처음까지 텍스트 복사 |
| `yG` | 커서부터 파일 끝까지 텍스트 복사 |
| `y%` | 커서부터 짝이 되는 괄호까지 텍스트 복사 |
| `yf{글자}` / `yt{글자}` | 커서부터 해당 글자까지 텍스트 복사 |
| `yiw` / `yaw` | 현재 단어 텍스트 복사 (안쪽 / 공백 포함) |
| `yi"` / `ya"` | 따옴표 안쪽 / 바깥쪽 텍스트 복사 |
| `yi(` / `ya(` | 괄호 안쪽 / 바깥쪽 텍스트 복사 |
| `yi{` / `ya{` | 중괄호 안쪽 / 바깥쪽 텍스트 복사 |
| `yip` / `yap` | 문단 안쪽 / 바깥쪽 텍스트 복사 |
| `{숫자}yy` | N줄 텍스트 복사 (예: `5yy`는 5줄 복사) |

### 변경 (Change)
| 단축키 | 설명 |
|--------|------|
| `s` | 커서 위치 글자 삭제 후 입력 모드 전환 (편집) |
| `S` 또는 `cc` | 현재 줄 전체 텍스트 변경 (편집) |
| `C` 또는 `c$` | 커서부터 줄 끝까지 텍스트 변경 (편집) |
| `c0` | 커서부터 줄 시작까지 텍스트 변경 |
| `c^` | 커서부터 줄 첫 글자까지 텍스트 변경 |
| `cw` / `cW` | 커서부터 다음 단어 시작까지 텍스트 변경 |
| `ce` | 커서부터 현재 단어 끝까지 텍스트 변경 |
| `cb` | 커서부터 이전 단어 시작까지 텍스트 변경 |
| `cj` / `ck` | 현재 줄과 아래 / 위 줄 텍스트 변경 |
| `cgg` / `cG` | 커서부터 파일 처음 / 끝까지 텍스트 변경 |
| `c%` | 커서부터 짝이 되는 괄호까지 텍스트 변경 |
| `cf{글자}` / `ct{글자}` | 커서부터 해당 글자까지 텍스트 변경 |
| `ciw` / `caw` | 현재 단어 텍스트 변경 (안쪽 / 공백 포함) |
| `ci"` / `ca"` | 따옴표 안쪽 / 바깥쪽 텍스트 변경 |
| `ci(` / `ca(` | 괄호 안쪽 / 바깥쪽 텍스트 변경 |
| `ci{` / `ca{` | 중괄호 안쪽 / 바깥쪽 텍스트 변경 |
| `cip` / `cap` | 문단 안쪽 / 바깥쪽 텍스트 변경 |
| `cit` / `cat` | HTML 태그 안쪽 / 바깥쪽 텍스트 변경 |
| `{숫자}cc` | N줄 텍스트 변경 (예: `2cc`는 2줄 변경) |

### 붙여넣기 (Paste)
| 단축키 | 설명 |
|--------|------|
| `p` | 커서 뒤(또는 아래 줄)에 텍스트 붙여넣기 |
| `P` | 커서 앞(또는 위 줄)에 텍스트 붙여넣기 |
| `gp` / `gP` | 텍스트 붙여넣기 후 커서를 끝으로 이동 |
| `]p` / `[p` | 현재 줄의 들여쓰기에 맞춰 텍스트 붙여넣기 |
| `"0p` | 최근 복사한 텍스트만 붙여넣기 (삭제 내용 제외) |
| `"+p` | 시스템 클립보드에서 텍스트 붙여넣기 |
| `"+y` | 시스템 클립보드로 텍스트 복사 |
| `"_d{모션}` | 블랙홀 레지스터로 텍스트 삭제 (레지스터 오염 없음) |

---

## 6. 되돌리기 / 다시 실행

| 단축키 | 설명 |
|--------|------|
| `u` | 직전 편집 변경 취소 (Undo, 되돌리기) |
| `<C-r>` | 취소한 편집 변경 다시 실행 (Redo) |
| `U` | 현재 줄의 모든 편집 변경 취소 |
| `:earlier 5m` | 5분 전 상태로 되돌리기 (시간 기반 Undo) |
| `:later 5m` | 5분 후 상태로 이동 (시간 기반 Redo) |
| `g-` / `g+` | 언두 트리 이전/다음 가지 이동 (분기 되돌리기) |

---

## 7. 검색과 치환

### 검색
| 단축키 | 설명 |
|--------|------|
| `/{패턴}` | 텍스트를 앞 방향으로 검색 (찾기) |
| `?{패턴}` | 텍스트를 뒷 방향으로 검색 (역방향 찾기) |
| `n` | 다음 검색 결과로 커서 이동 |
| `N` | 이전 검색 결과로 커서 이동 |
| `*` | 커서 위치 단어를 앞으로 검색 (찾기) |
| `#` | 커서 위치 단어를 뒤로 검색 (역방향 찾기) |
| `g*` / `g#` | 커서 위치 단어 부분 일치 검색 |
| `:nohlsearch` | 검색 하이라이트 해제 (찾기 강조 끄기) |

### 검색 패턴 조합 (복수 단어 검색)
| 패턴 | 설명 |
|------|------|
| `/단어1\|단어2` | 두 단어 중 하나라도 검색 (OR 찾기) |
| `/단어1.*단어2` | 같은 줄에서 단어1 뒤에 단어2가 오는 패턴 검색 |
| `/단어1\s\+단어2` | 단어1과 단어2 사이에 공백이 있는 패턴 검색 |

### 치환
| 명령 | 설명 |
|------|------|
| `:s/old/new/` | 현재 줄의 첫 일치 텍스트 치환 (바꾸기) |
| `:s/old/new/g` | 현재 줄의 모든 일치 텍스트 치환 (바꾸기) |
| `:%s/old/new/g` | 파일 전체 텍스트 치환 (바꾸기) |
| `:%s/old/new/gc` | 파일 전체 텍스트 치환, 확인 후 바꾸기 |
| `:'<,'>s/old/new/g` | 비주얼 선택 영역 텍스트 치환 (바꾸기) |

### 프로젝트 전체 검색 치환 (vimgrep + cfdo, 여러 파일 일괄 바꾸기)

**순서: 검색 → quickfix 확인 → 치환**

```vim
" 1. 프로젝트 전체 파일에서 단어 검색 (quickfix 목록에 결과 로드)
:vimgrep /검색할단어/g **/*

" 2. quickfix 목록 열어 검색 결과 확인
:copen

" 3. 모든 파일에서 하나씩 확인하며 텍스트 치환 (바꾸기)
:cfdo %s/검색할단어/바꿀단어/gc | update
```

| 명령 | 설명 |
|------|------|
| `:vimgrep /패턴/g **/*` | 프로젝트 전체 파일에서 텍스트 검색 (찾기) |
| `:copen` | quickfix 검색 결과 목록 창 열기 |
| `:cclose` | quickfix 검색 결과 목록 창 닫기 |
| `:cnext` / `:cprev` | quickfix 다음 / 이전 검색 결과로 커서 이동 |
| `:cfdo %s/old/new/gc \| update` | 모든 파일에서 확인하며 텍스트 치환 후 저장 (일괄 바꾸기) |

**확인(c) 모드 응답 키**

| 키 | 설명 |
|----|------|
| `y` | 현재 매치 텍스트 변경 (바꾸기) |
| `n` | 현재 매치 건너뛰기 (유지) |
| `a` | 이후 모든 매치 텍스트 변경 (전체 바꾸기) |
| `q` | 치환 중단 (종료) |
| `l` | 현재 매치 텍스트 변경 후 치환 중단 |

---

## 8. 텍스트 오브젝트

> 오퍼레이터(`d`, `c`, `y`, `v` 등)와 조합해서 사용합니다.
> 예: `diw`(단어 삭제), `ci"`(따옴표 안쪽 변경), `ya{`(중괄호 포함 복사)

### 안쪽 / 바깥쪽
| 오브젝트 | 의미 |
|----------|------|
| `iw` / `aw` | 단어 텍스트 선택 (inner / around) |
| `iW` / `aW` | 단어 텍스트 선택 (공백 기준) |
| `is` / `as` | 문장 텍스트 선택 |
| `ip` / `ap` | 문단 텍스트 선택 |

### 괄호 / 따옴표
| 오브젝트 | 의미 |
|----------|------|
| `i"` / `a"` | 큰따옴표 안쪽/바깥쪽 텍스트 선택 |
| `i'` / `a'` | 작은따옴표 안쪽/바깥쪽 텍스트 선택 |
| `` i` `` / `` a` `` | 백틱 안쪽/바깥쪽 텍스트 선택 |
| `i(` / `a(` 또는 `ib` / `ab` | 소괄호 안쪽/바깥쪽 텍스트 선택 |
| `i[` / `a[` | 대괄호 안쪽/바깥쪽 텍스트 선택 |
| `i{` / `a{` 또는 `iB` / `aB` | 중괄호 안쪽/바깥쪽 텍스트 선택 |
| `i<` / `a<` | 꺾쇠 괄호 안쪽/바깥쪽 텍스트 선택 |
| `it` / `at` | HTML/XML 태그 안쪽/바깥쪽 텍스트 선택 |

---

## 9. 비주얼 모드

| 단축키 | 설명 |
|--------|------|
| `v` | 문자 단위 비주얼 모드 전환 (텍스트 선택) |
| `V` | 줄 단위 비주얼 모드 전환 (라인 선택) |
| `<C-v>` | 블록(사각형) 비주얼 모드 전환 (영역 선택) |
| `gv` | 이전 선택 영역 복원 (다시 선택) |
| `o` | 선택 영역의 반대편 끝으로 커서 이동 |
| `d` / `x` | 선택 영역 텍스트 삭제 |
| `y` | 선택 영역 텍스트 복사 |
| `c` | 선택 영역 텍스트 변경 (편집) |
| `>` / `<` | 선택 영역 들여쓰기 증가/감소 (인덴트) |
| `=` | 선택 영역 자동 들여쓰기 (포맷) |
| `u` / `U` | 선택 영역 텍스트 소문자/대문자 변환 |
| `~` | 선택 영역 텍스트 대소문자 토글 |

### 비주얼 블록 모드 팁
| 동작 | 설명 |
|------|------|
| `<C-v>` → 이동 → `I` → 입력 → `<Esc>` | 여러 줄 앞에 텍스트 동시 삽입 (멀티커서) |
| `<C-v>` → 이동 → `A` → 입력 → `<Esc>` | 여러 줄 뒤에 텍스트 동시 삽입 (멀티커서) |
| `<C-v>` → 이동 → `c` → 입력 → `<Esc>` | 여러 줄 텍스트 동시 변경 (멀티커서) |

---

## 10. 마크(Mark)

| 단축키 | 설명 |
|--------|------|
| `m{글자}` | 현재 커서 위치에 마크 저장 (a-z: 버퍼 내, A-Z: 전역) |
| `` `{글자} `` | 해당 마크 위치로 커서 정확히 이동 |
| `'{글자}` | 해당 마크가 있는 줄로 커서 이동 |
| `` `` `` | 직전 점프 위치로 커서 이동 |
| `` `. `` | 마지막 편집 위치로 커서 이동 |
| `` `^ `` | 마지막 입력 모드 종료 위치로 커서 이동 |
| `:marks` | 모든 마크 목록 보기 |

---

## 11. 레지스터(Register)

| 단축키 | 설명 |
|--------|------|
| `"{레지스터}{명령}` | 특정 레지스터로 텍스트 저장/읽기 (예: `"ay`, `"ap`) |
| `"0` | 최근 yank 레지스터, 복사 텍스트 전용 (삭제 제외) |
| `"+` | 시스템 클립보드 레지스터 (복사/붙여넣기) |
| `"*` | 선택 클립보드 레지스터 (X11) |
| `"_` | 블랙홀 레지스터, 텍스트 저장하지 않음 (삭제 전용) |
| `""` | 기본(무명) 레지스터 |
| `"%` | 현재 파일 이름 레지스터 |
| `":` | 마지막 명령 모드 입력 레지스터 |
| `".` | 마지막 삽입한 텍스트 레지스터 |
| `:reg` 또는 `:registers` | 모든 레지스터 내용 확인 (목록 보기) |

---

## 12. 매크로(Macro)

| 단축키 | 설명 |
|--------|------|
| `q{글자}` | 매크로 녹화 시작 (해당 레지스터에 저장) |
| `q` | 매크로 녹화 종료 |
| `@{글자}` | 해당 레지스터의 매크로 실행 (반복 재생) |
| `@@` | 직전 실행한 매크로 다시 실행 (반복) |
| `{숫자}@{글자}` | 매크로를 N번 반복 실행 (예: `10@q`) |

---

## 13. 창(Window) 관리

### 창 열기 / 닫기
| 단축키 | 설명 |
|--------|------|
| `<C-w>s` 또는 `:split` | 화면 수평 분할 (창 열기) |
| `<C-w>v` 또는 `:vsplit` | 화면 수직 분할 (창 열기) |
| `<C-w>n` | 새 창에서 빈 버퍼 열기 |
| `<C-w>c` 또는 `:close` | 현재 창 닫기 |
| `<C-w>o` 또는 `:only` | 현재 창만 남기고 나머지 닫기 |
| `<C-w>q` | 현재 창 종료 (닫기) |

### 창 이동
| 단축키 | 설명 |
|--------|------|
| `<C-w>h` / `<C-w>j` / `<C-w>k` / `<C-w>l` | 왼쪽 / 아래 / 위 / 오른쪽 창으로 커서 이동 |
| `<C-w>w` | 다음 창으로 커서 순환 이동 |
| `<C-w>p` | 직전 창으로 커서 이동 |
| `<C-w>t` / `<C-w>b` | 최상단 / 최하단 창으로 커서 이동 |

### 창 배치 변경
| 단축키 | 설명 |
|--------|------|
| `<C-w>H` / `<C-w>J` / `<C-w>K` / `<C-w>L` | 창을 화면 왼/아래/위/오른쪽 끝으로 배치 이동 |
| `<C-w>r` | 창 순서 회전 (배치 변경) |
| `<C-w>x` | 인접한 창과 위치 교환 (배치 변경) |
| `<C-w>T` | 현재 창을 새 탭으로 분리 (이동) |

### 창 크기 조절
| 단축키 | 설명 |
|--------|------|
| `<C-w>=` | 모든 창 크기 동일하게 조절 |
| `<C-w>_` | 현재 창 높이 최대화 (크기 조절) |
| `<C-w>|` | 현재 창 너비 최대화 (크기 조절) |
| `<C-w>+` / `<C-w>-` | 현재 창 높이 증가 / 감소 (크기 조절) |
| `<C-w>>` / `<C-w><` | 현재 창 너비 증가 / 감소 (크기 조절) |

---

## 14. 탭(Tab) 관리

| 명령 / 단축키 | 설명 |
|---------------|------|
| `:tabnew {파일}` | 새 탭에서 파일 열기 |
| `:tabedit {파일}` | 새 탭에서 파일 편집 (열기) |
| `:tabclose` | 현재 탭 닫기 |
| `:tabonly` | 현재 탭만 남기고 나머지 닫기 |
| `gt` 또는 `:tabnext` | 다음 탭으로 전환 (이동) |
| `gT` 또는 `:tabprevious` | 이전 탭으로 전환 (이동) |
| `{숫자}gt` | N번 탭으로 전환 (이동) |
| `:tabmove {N}` | 탭 위치 이동 (순서 변경) |

---

## 15. 버퍼(Buffer) 관리

| 명령 | 설명 |
|------|------|
| `:e {파일}` | 파일 열기 (버퍼에 로드) |
| `:enew` | 새 빈 버퍼 열기 |
| `:ls` 또는 `:buffers` | 버퍼 목록 보기 (파일 리스트) |
| `:b {번호\|이름}` | 특정 버퍼로 전환 (파일 이동) |
| `:bn` / `:bnext` | 다음 버퍼로 전환 (파일 이동) |
| `:bp` / `:bprevious` | 이전 버퍼로 전환 (파일 이동) |
| `:bd` / `:bdelete` | 현재 버퍼 삭제 (파일 닫기) |
| `:bw` / `:bwipeout` | 버퍼 완전 제거 (파일 닫기) |
| `<C-^>` 또는 `<C-6>` | 직전 버퍼로 전환 (파일 이동) |

---

## 16. 폴딩(Folding)

| 단축키 | 설명 |
|--------|------|
| `zf{모션}` | 해당 영역을 수동 폴드로 생성 (접기 만들기) |
| `zo` | 커서 위치 폴드 열기 (펼치기) |
| `zc` | 커서 위치 폴드 닫기 (접기) |
| `za` | 커서 위치 폴드 토글 (접기/펼치기 전환) |
| `zO` | 커서 위치 폴드 모두 열기 (재귀 펼치기) |
| `zC` | 커서 위치 폴드 모두 닫기 (재귀 접기) |
| `zA` | 커서 위치 폴드 재귀 토글 (접기/펼치기) |
| `zR` | 파일 내 모든 폴드 열기 (전체 펼치기) |
| `zM` | 파일 내 모든 폴드 닫기 (전체 접기) |
| `zd` | 커서 위치 폴드 삭제 (접기 제거) |
| `zE` | 파일 내 모든 폴드 삭제 (접기 전체 제거) |
| `zj` / `zk` | 다음 / 이전 폴드로 커서 이동 |

---

## 17. 명령 모드(Command-line)

| 명령 | 설명 |
|------|------|
| `:` | 명령 모드 진입 (전환) |
| `:!{shell 명령}` | 셸 명령 실행 (터미널, 예: `:!ls`) |
| `:r {파일}` | 커서 위치에 파일 내용 삽입 (읽기) |
| `:r !{명령}` | 셸 명령 결과를 커서 위치에 삽입 |
| `q:` | 명령 모드 히스토리 창 열기 (기록 보기) |
| `q/` / `q?` | 검색 히스토리 창 열기 (기록 보기) |
| `<C-f>` (명령 모드 중) | 명령 히스토리 편집 창 진입 |
| `<Up>` / `<Down>` | 이전 / 다음 명령 히스토리 탐색 |
| `<C-r>{레지스터}` | 레지스터 내용을 명령 모드에 텍스트 삽입 |

---

## 18. 파일 관련

| 명령 | 설명 |
|------|------|
| `:w` | 현재 파일 저장 (쓰기) |
| `:w {파일}` | 다른 이름으로 파일 저장 (쓰기) |
| `:wa` | 모든 버퍼 파일 저장 |
| `:q` | 현재 창 종료 (닫기) |
| `:q!` | 변경사항 무시하고 강제 종료 (닫기) |
| `:wq` 또는 `:x` | 파일 저장 후 종료 (닫기) |
| `:qa` | 모든 창 종료 (전체 닫기) |
| `:wqa` | 모든 창 파일 저장 후 종료 (전체 닫기) |
| `:e!` | 현재 파일 다시 불러오기 (변경사항 버림, 되돌리기) |
| `:saveas {파일}` | 다른 이름으로 파일 저장 후 전환 |
| `:pwd` | 현재 작업 디렉토리 경로 출력 |
| `:cd {경로}` | 작업 디렉토리 경로 변경 |

---

## 19. 도움말

| 명령 | 설명 |
|------|------|
| `:help` | Neovim 도움말 메인 페이지 열기 |
| `:help {주제}` | 특정 주제의 도움말 검색 (예: `:help motion`) |
| `:help quickref` | 주요 명령 요약 도움말 (참조) |
| `:help index` | 모든 명령 전체 목록 도움말 |
| `:helpgrep {패턴}` | 도움말 전체 텍스트 검색 (찾기) |
| `<C-]>` (도움말 내) | 태그 점프, 링크 따라가기 (이동) |
| `<C-t>` (도움말 내) | 이전 태그로 돌아가기 (뒤로 이동) |
| `:checkhealth` | Neovim 환경 상태 점검 (진단) |
| `:version` | Neovim 버전 정보 확인 |

---

## 핵심 개념: 오퍼레이터 + 모션

Vim의 편집은 **오퍼레이터(Operator) + 모션(Motion)** 조합이 기본입니다.

```
{숫자}{오퍼레이터}{모션 또는 텍스트 오브젝트}
```

### 주요 오퍼레이터
| 오퍼레이터 | 의미 |
|------------|------|
| `d` | 삭제 (delete) |
| `c` | 변경 (change) |
| `y` | 복사 (yank) |
| `>` / `<` | 들여쓰기 증가 / 감소 |
| `=` | 자동 들여쓰기 |
| `gU` / `gu` | 대문자 / 소문자 |
| `g~` | 대소문자 토글 |

### 조합 예시
| 조합 | 동작 |
|------|------|
| `d2w` | 단어 2개 텍스트 삭제 |
| `c3j` | 아래 3줄 텍스트 변경 |
| `y$` | 커서부터 줄 끝까지 텍스트 복사 |
| `diw` | 현재 단어 전체 텍스트 삭제 |
| `ci"` | 따옴표 안쪽 텍스트 변경 |
| `ya{` | 중괄호 포함 텍스트 복사 |
| `>ap` | 현재 문단 들여쓰기 증가 |
| `gUiw` | 현재 단어 대문자 변환 |
| `=G` | 커서부터 파일 끝까지 자동 들여쓰기 |

이 규칙만 이해하면 수많은 조합을 직관적으로 만들어낼 수 있습니다.

---

## 더 알아보기

- `:help` 명령으로 언제든 공식 문서를 참조할 수 있습니다
- `:help user-manual`로 Vim 사용자 매뉴얼 전체를 볼 수 있습니다
- 실습 추천: `vimtutor` (터미널에서 실행)
